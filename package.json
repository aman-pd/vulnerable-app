{
  "name": "vulnerable-app",
  "version": "1.0.0",
  "description": "Intentionally vulnerable app for GHA supply chain attack training",
  "main": "index.js",
  "scripts": {
    "test": "echo 'Running tests...' && exit 0",
    "build": "echo 'Building project...' && mkdir -p dist && echo 'Build complete' > dist/output.txt",
    "lint": "echo 'Linting...' && exit 0"
  },
  "dependencies": {},
  "devDependencies": {}
}
